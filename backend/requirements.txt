# Optimeringsmotor (spopt) — backend/batch, ej i frontend-bundeln.
# Installera:  pip install -r backend/requirements.txt
spopt>=0.6
pulp>=2.7
numpy>=1.24
pandas>=2.0
duckdb>=1.0
fastapi>=0.110
uvicorn>=0.29
